![Ubuntu 24.04](https://img.shields.io/badge/Ubuntu-24.04-orange)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://github.com/bivlked/amneziawg-installer/blob/main/LICENSE) <!-- Убедитесь, что файл LICENSE существует -->
![Status](https://img.shields.io/badge/Status-Active-success)
[![Version](https://img.shields.io/badge/Version-2.0-blue)](https://github.com/bivlked/amneziawg-installer/releases) <!-- Обновляйте версию -->

<p align="center">
  <img src="https://www.bondarev.net/awg_logo.jpg" alt="AmneziaWG Installer Logo" width="250">
</p>

<h1 align="center">Автоматическая установка и управление AmneziaWG</h1>

<p align="center">
  Набор Bash-скриптов для быстрой, безопасной и удобной установки,<br>
  настройки и управления VPN-сервером <strong>AmneziaWG</strong> на <strong>Ubuntu 24.04 LTS Minimal</strong>.
</p>

<p align="center">
  <a href="#✨-возможности">Возможности</a> •
  <a href="#🚀-быстрый-старт">Быстрый старт</a> •
  <a href="#🖥️-требования">Требования</a> •
  <a href="#🔧-установка">Установка</a> •
  <a href="#👥-управление-клиентами">Управление</a> •
  <a href="#⚙️-детали-конфигурации">Конфигурация</a> •
  <a href="#❓-faq">FAQ</a> •
  <a href="#📝-лицензия">Лицензия</a>
</p>

---

## ✨ Возможности

*   🚀 **Быстрая установка:** Развертывание и настройка сервера AmneziaWG одной командой.
*   🔄 **Возобновляемость:** Установка автоматически продолжается с нужного шага после перезагрузок или сбоев.
*   🔒 **Безопасность по умолчанию:**
    *   Автоматическая настройка фаервола `UFW` (разрешены только SSH и порт VPN, используются лимиты для SSH).
    *   Отключение IPv6 (рекомендуется, но опционально).
    *   Установка строгих прав доступа (`chmod 600/700`) к ключам и конфигурациям.
    *   Применение рекомендуемых настроек безопасности ядра (`sysctl`).
    *   Автоматическая установка и базовая настройка `Fail2Ban` для защиты SSH.
*   ⚙️ **Надежная конфигурация:**
    *   Автоматическое включение репозиториев `deb-src`.
    *   Установка всех зависимостей, включая DKMS модуль для поддержки обновлений ядра.
    *   Проверка корректной загрузки и `vermagic` модуля ядра `amneziawg`.
*   🔧 **Гибкие настройки клиента:**
    *   DNS по умолчанию: `1.1.1.1` (Cloudflare).
    *   PersistentKeepalive по умолчанию: `33` секунды.
    *   **Выбор режима маршрутизации** при установке:
        *   Стандартный (весь трафик через VPN, `0.0.0.0/0`).
        *   Amnezia (список диапазонов + принудительное туннелирование DNS, по умолчанию).
        *   Пользовательский (только указанные сети).
*   🧑‍💻 **Удобное управление клиентами:**
    *   Отдельный скрипт `manage_amneziawg.sh` (скачивается автоматически).
    *   Команды для добавления (`add`), удаления (`remove`), просмотра (`list`), перегенерации файлов (`regen`).
    *   Дополнительные команды для управления сервером (`show`, `check`, `restart`, `backup`, `restore`, `modify`).
*   📦 **Резервное копирование:** Встроенная команда для создания и восстановления резервных копий конфигураций. Автоматическое ежедневное резервное копирование (опционально).
*   🔄 **Автоматизация обслуживания:** Настройка автоматических обновлений безопасности и ротации логов.
*   🩺 **Диагностика:** Возможность создания подробного диагностического отчета (`--diagnostic`).
*   🗑️ **Деинсталляция:** Возможность полного удаления AmneziaWG и его файлов (`--uninstall`).
*   📝 **Подробное логирование:** Все шаги записываются в лог-файлы в `/root/awg/`.

---

## 🚀 Быстрый старт

Подключитесь к вашему **чистому серверу Ubuntu 24.04 LTS Minimal** по SSH и выполните **одну команду**:

```bash
wget -O - https://raw.githubusercontent.com/bivlked/amneziawg-installer/main/install_amneziawg.sh | sudo bash
```

> **ВНИМАНИЕ:** Эта команда скачает и немедленно запустит скрипт установки с правами `root`. Убедитесь, что вы доверяете источнику скрипта. Всегда проверяйте URL перед выполнением.

---

## 🖥️ Требования

*   **ОС:** Чистая установка **Ubuntu Server 24.04 LTS Minimal**.
*   **Доступ:** Права `root` (через `sudo` или прямой логин).
*   **Интернет:** Стабильное подключение.
*   **Ресурсы:** ~2-3 ГБ диска, ~512+ МБ ОЗУ.
*   **SSH:** Доступ по SSH. **Критически важно:** Если используется **нестандартный порт SSH** (отличный от 22), **ОБЯЗАТЕЛЬНО** добавьте правило `sudo ufw allow ВАШ_ПОРТ/tcp` **ДО** запуска скрипта!

---

## 🔧 Установка (`install_amneziawg.sh`)

Скрипт выполняет полную установку и настройку.

1.  **Запустите скрипт** командой из раздела "Быстрый старт".
2.  **Начальная настройка:** При первом запуске скрипт интерактивно запросит:
  UDP порт для AmneziaWG (по умолчанию: `39743`).
  Внутреннюю подсеть VPN (по умолчанию: `10.9.9.1/24`).
  Отключать ли IPv6 (по умолчанию: `Да`).
  Режим маршрутизации `AllowedIPs` (по умолчанию: `Amnezia`).
жмите `Enter` для выбора значения по умолчанию. Настройки сохранятся в `/root/awg/setup.conf`. Вы также можете задать эти параметры неинтерактивно через аргументы командной строки (см. `install_amneziawg.sh --help`).
3.  **Перезагрузки:** Скрипту потребуется **ДВЕ** перезагрузки. Он выведет **ЗАМЕТНОЕ** предупреждение и запросит подтверждение `[y/N]`. Введите `y` для перезагрузки.
4.  **Продолжение:** После каждой перезагрузки **снова запустите ту же самую команду**:
`bash
et -O - https://raw.githubusercontent.com/bivlked/amneziawg-installer/main/install_amneziawg.sh | sudo bash
`
рипт автоматически продолжит с нужного шага.
5.  **Завершение:** Вы увидите сообщение `Установка и настройка AmneziaWG УСПЕШНО ЗАВЕРШЕНА!`.

**Расположение файлов:**

*   Рабочая директория: `/root/awg/`
*   Конфигурация сервера: `/etc/amnezia/amneziawg/awg0.conf`
*   Клиентские `.conf` и `.png` файлы: `/root/awg/`
*   Логи установки и управления: `/root/awg/*.log`
*   Скрипт управления: `/root/awg/manage_amneziawg.sh` (скачивается автоматически)

## 👥 Управление клиентами (`manage_amneziawg.sh`)

Используйте этот скрипт для повседневного управления клиентами.

**Использование:**

```bash
sudo bash /root/awg/manage_amneziawg.sh <команда> [аргументы]
```

**Основные команды:**

| Команда             | Аргументы                       | Описание                                                                 | Требуется перезапуск? |
| :------------------ | :------------------------------ | :----------------------------------------------------------------------- | :--------------------: |
| `add`               | `<имя_клиента>`                 | Добавить нового клиента                                                  |          **Да**        |
| `remove`            | `<имя_клиента>`                 | Удалить существующего клиента                                            |          **Да**        |
| `list`              | `[--verbose]`                   | Показать список клиентов (с доп. инфо при `--verbose`)                   |           Нет          |
| `regen`             | `[имя_клиента]`                 | Перегенерировать `.conf` и `.png` файлы (без имени - для всех)           |           Нет          |
| `show`              |                                 | Показать текущий статус AmneziaWG (`awg show`)                           |           Нет          |
| `check` / `status`  |                                 | Проверить общее состояние сервера AmneziaWG                              |           Нет          |
| `modify`            | `<имя> <параметр> <значение>`   | Изменить параметр в `.conf` файле клиента (например, `AllowedIPs`)      |           Нет          |
| `backup`            |                                 | Создать резервную копию конфигураций в `/root/awg/backups/`            |           Нет          |
| `restore`           | `[путь_к_бэкапу]`               | Восстановить конфигурацию из бэкапа (интерактивный выбор если путь не указан) |          **Да**        |
| `restart`           |                                 | Перезапустить сервис `awg-quick@awg0`                                    |           -          |
| `help`              |                                 | Показать справку                                                         |           Нет          |

> **❗️ ВАЖНО:** После выполнения команд `add`, `remove` или `restore` **обязательно** перезапустите сервис AmneziaWG для применения изменений:
> ```bash
> sudo systemctl restart awg-quick@awg0
> ```
> (Или используйте команду `sudo bash /root/awg/manage_amneziawg.sh restart`)

**Перенос файлов клиентам:**

Используйте `scp` или `sftp` для копирования `.conf` или `.png` файлов из `/root/awg/` на ваши устройства. Пример команды (выполнять на локальном ПК):
```bash
scp root@<IP_АДРЕС_СЕРВЕРА>:/root/awg/имя_клиента.conf ./
scp root@<IP_АДРЕС_СЕРВЕРА>:/root/awg/имя_клиента.png ./
```
**Обращайтесь с этими файлами осторожно!**

---

## ⚙️ Детали конфигурации и настройки

*   **Кастомизация клиента (по умолчанию):**
    *   `DNS = 1.1.1.1` (Cloudflare)
    *   `PersistentKeepalive = 33`
    *   `AllowedIPs = 0.0.0.0/5, ..., 8.8.8.8/32, 1.1.1.1/32` (Режим Amnezia)
*   **Изменение настроек по умолчанию:** Вы можете изменить значения DNS, Keepalive или `AllowedIPs`, которые применяются ко всем *новым* клиентам, отредактировав соответствующие команды `sed` в функции `step6_generate_configs` скрипта `install_amneziawg.sh` **перед его первым запуском**.
*   **Изменение настроек существующего клиента:** Используйте команду `manage_amneziawg.sh modify <имя> <параметр> <значение>`.
*   **AllowedIPs:**
    *   **Режим Amnezia (по умолчанию):** Использует сложный список диапазонов + DNS. Предположительно для обхода DPI. **Не исключает** локальную сеть клиента.
    *   **Режим "Весь трафик" (`0.0.0.0/0`):** Направляет весь IPv4 трафик через VPN. Максимальная приватность, но может блокировать доступ к локальной сети. Можно выбрать при установке или установить командой `modify`:
        ```bash
        sudo bash /root/awg/manage_amneziawg.sh modify <имя_клиента> AllowedIPs "0.0.0.0/0"
        ```
    *   **Режим "Только указанные сети" (Split-Tunneling):** Позволяет направить через VPN только трафик к определенным сетям (например, к рабочей или домашней). Можно выбрать при установке или установить командой `modify`:
        ```bash
        sudo bash /root/awg/manage_amneziawg.sh modify <имя_клиента> AllowedIPs "192.168.1.0/24, 10.8.0.0/16"
        ```
    *   **Исключение сайтов (*.ru и т.д.):** Этот скрипт **не поддерживает** исключение трафика к конкретным доменам из туннеля из-за сложности отслеживания их IP-адресов. Для этого требуется ручная настройка маршрутизации на клиенте.

---

## ❓ FAQ (Часто задаваемые вопросы)

<details>
  <summary><strong>В: Как изменить порт AmneziaWG после установки?</strong></summary>

  **О:**
  1.  Отредактируйте `/etc/amnezia/amneziawg/awg0.conf`, измените `ListenPort`.
  2.  Отредактируйте `/root/awg/setup.conf`, измените `AWG_PORT`.
  3.  Обновите правило UFW:
      ```bash
      sudo ufw delete allow СТАРЫЙ_ПОРТ/udp
      sudo ufw allow НОВЫЙ_ПОРТ/udp comment "AmneziaWG VPN"
      ```
  4.  Перезапустите сервис: `sudo systemctl restart awg-quick@awg0`
  5.  **Перегенерируйте** файлы конфигурации для **всех** клиентов, чтобы они узнали новый порт: `sudo bash /root/awg/manage_amneziawg.sh regen` и передайте обновленные файлы клиентам.
</details>

<details>
  <summary><strong>В: Как изменить внутреннюю подсеть VPN после установки?</strong></summary>

  **О:** Это более сложная операция. Проще всего выполнить деинсталляцию (`install_amneziawg.sh --uninstall`) и запустить установку заново, указав новую подсеть. Либо вручную отредактировать `Address` в `/etc/amnezia/amneziawg/awg0.conf`, `AWG_TUNNEL_SUBNET` в `/root/awg/setup.conf`, а затем **удалить и заново добавить всех клиентов** через `manage_amneziawg.sh`.
</details>

<details>
  <summary><strong>В: Будет ли работать AmneziaWG после обновления ядра?</strong></summary>

  **О:** Да, благодаря использованию DKMS, модуль ядра `amneziawg` должен автоматически пересобираться при обновлении ядра системы. Рекомендуется проверять статус после обновлений (`dkms status`).
</details>

<details>
  <summary><strong>В: Как полностью удалить AmneziaWG, установленный этим скриптом?</strong></summary>

  **О:** Запустите скрипт установки с флагом `--uninstall`:
  ```bash
  sudo bash /путь/к/install_amneziawg.sh --uninstall
  ```
  Или, если скрипт был удален:
  ```bash
  wget -O - https://raw.githubusercontent.com/bivlked/amneziawg-installer/main/install_amneziawg.sh | sudo bash -s -- --uninstall
  ```
  Скрипт запросит подтверждение и попытается удалить пакеты, конфигурации, правила фаервола, скрипты и сервисы, связанные с этой установкой.
</details>

---

## 🛠️ Устранение неполадок

1.  **Проверьте лог установки:** `/root/awg/install_amneziawg.log`
2.  **Проверьте лог управления:** `/root/awg/manage_amneziawg.log`
3.  **Проверьте статус сервиса:** `sudo systemctl status awg-quick@awg0`
4.  **Проверьте журнал сервиса:** `sudo journalctl -u awg-quick@awg0 -n 50 --no-pager`
5.  **Проверьте статус WireGuard:** `sudo awg show`
6.  **Проверьте статус фаервола:** `sudo ufw status verbose`
7.  **Создайте диагностический отчет:**
`bash
do bash /путь/к/install_amneziawg.sh --diagnostic
Отчет будет сохранен в /root/awg/diagnostic_report_...txt
`

---

## 📝 Лицензия и Автор

*   **Автор скриптов:** @bivlked [GitHub](https://github.com/bivlked)
*   **Лицензия:** MIT License (см. файл `LICENSE`)

### Благодарности
*   Команде [Amnezia VPN](https://github.com/amnezia-vpn) за AmneziaWG.
*   Пользователю [remittor](https://gist.github.com/remittor) за основу утилиты `awgcfg.py`.

---

<p align="center">
  <a href="#автоматическая-установка-и-управление-amneziawg-на-ubuntu-2404">↑ К началу</a>
</p>
