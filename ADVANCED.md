# AmneziaWG Installer: Дополнительная информация и настройки

Это дополнение к основному [README.md](README.md), содержащее более глубокие технические детали, пояснения и продвинутые опции для скриптов установки и управления AmneziaWG.

## Оглавление

<a id="toc-adv"></a>
- [✨ Возможности (Подробно)](#features-detailed-adv)
- [⚙️ Детали конфигурации клиента](#config-details-adv)
  - [AllowedIPs](#allowedips-adv)
  - [PersistentKeepalive](#persistentkeepalive-adv)
  - [DNS](#dns-adv)
  - [Изменение настроек по умолчанию](#change-defaults-adv)
- [🔒 Настройки безопасности сервера](#security-adv)
  - [Фаервол UFW](#ufw-adv)
  - [Параметры ядра (Sysctl)](#sysctl-adv)
  - [Fail2Ban (Автоматическая установка)](#fail2ban-adv)
- [🔄 Автоматизация обслуживания (Ограниченная)](#automation-adv)
- [⚙️ CLI Параметры запуска скриптов](#cli-params-adv)
  - [install_amneziawg.sh](#install-cli-adv)
  - [manage_amneziawg.sh](#manage-cli-adv)
- [🧑‍💻 Полный список команд управления](#manage-commands-adv)
- [🛠️ Технические детали](#tech-details-adv)
  - [DKMS](#dkms-adv)
  - [Python Venv](#python-venv-adv)
  - [awgcfg.py и Workaround](#awgcfgpy-workaround-adv)
- [❓ FAQ (Дополнительные вопросы)](#faq-advanced-adv)
- [🩺 Диагностика и деинсталляция](#diag-uninstall-adv)
- [🤝 Внесение вклада (Contributing)](#contributing-adv)
- [💖 Благодарности](#thanks-adv)

---

<a id="features-detailed-adv"></a>
## ✨ Возможности (Подробно)

* **Автоматическая установка:** Устанавливает AmneziaWG, DKMS модуль, зависимости, Python окружение, настраивает сеть, фаервол, sysctl.
* **Возобновляемость:** Использует файл состояния (`/root/awg/setup_state`) для продолжения после обязательных перезагрузок.
* **Интерактивность:** При первом запуске запрашивает основные параметры, позволяя использовать значения по умолчанию.
* **Безопасность по умолчанию:**
    * `UFW`: Включается с политикой `deny incoming`, разрешает только SSH (с `limit`) и порт VPN.
    * `IPv6`: По умолчанию предлагается отключить через `sysctl`.
    * `Права доступа`: Устанавливаются строгие права (600/700).
    * `Sysctl`: Применяются параметры для harden'инга TCP/IP, защиты от спуфинга и ICMP-атак.
    * `Fail2Ban`: Автоматически устанавливается и настраивается для защиты SSH.
* **Надежность:** Установка модуля ядра через DKMS, проверки `vermagic`.
* **Гибкость:** Возможность задать параметры через CLI для неинтерактивной установки.
* **Управление:** Скрипт `manage_amneziawg.sh` для управления клиентами, бэкапами, проверки статуса.
* **Резервное копирование:** Команда `backup` в скрипте управления. (*Автоматическое ежедневное копирование убрано*).
* **Автоматизация:** Автоматическая установка Fail2Ban. (*Авто-обновления и ротация логов убраны*).
* **Диагностика и Деинсталляция:** Опции `--diagnostic` и `--uninstall`.

---

<a id="config-details-adv"></a>
## ⚙️ Детали конфигурации клиента

<a id="allowedips-adv"></a>
### AllowedIPs

Определяет, какой трафик **клиент** направляет в VPN-туннель.

1.  **Режим 1: Весь трафик (`0.0.0.0/0`)**
    * Весь IPv4 трафик клиента -> VPN.
    * Максимальная приватность. Может блокировать доступ к LAN.

2.  **Режим 2: Список Amnezia + DNS (По умолчанию)**
    * Список публичных IP-диапазонов + DNS `1.1.1.1`, `8.8.8.8`.
    * **Цель:** Обход DPI, туннелирование DNS. Рекомендуется.
    * Список: `0.0.0.0/5, ..., 8.8.8.8/32, 1.1.1.1/32`

3.  **Режим 3: Пользовательский (Split-Tunneling)**
    * Только трафик к указанным сетям -> VPN.
    * Пример: `192.168.1.0/24,10.50.0.0/16`

**Калькулятор AllowedIPs:** [WireGuard AllowedIPs Calculator](https://www.procustodibus.com/blog/2021/03/wireguard-allowedips-calculator/).

<a id="persistentkeepalive-adv"></a>
### PersistentKeepalive

* **Значение по умолчанию:** `33` секунды.
* Поддерживает UDP-сессию через NAT.
* **Изменение:** `sudo bash /root/awg/manage_amneziawg.sh modify <имя> PersistentKeepalive 25`

<a id="dns-adv"></a>
### DNS

* **Значение по умолчанию:** `1.1.1.1` (Cloudflare).
* DNS-сервер для клиента внутри VPN.
* **Изменение:** `sudo bash /root/awg/manage_amneziawg.sh modify <имя> DNS "8.8.8.8,1.0.0.1"`

<a id="change-defaults-adv"></a>
### Изменение настроек по умолчанию

Отредактируйте скрипт **`install_amneziawg.sh`** *перед* первым запуском:

1.  Найдите `step6_generate_configs` -> команды `sed` для `DNS`, `PersistentKeepalive`.
2.  Найдите `configure_routing_mode` -> переменная `ALLOWED_IPS` в блоке `*)` для режима 2.

---

<a id="security-adv"></a>
## 🔒 Настройки безопасности сервера

<a id="ufw-adv"></a>
### Фаервол UFW

* **Политики:** Deny incoming, Allow outgoing.
* **Правила:** `limit 22/tcp` (SSH), `allow <порт_vpn>/udp`.
* **Проверка:** `sudo ufw status verbose`

<a id="sysctl-adv"></a>
### Параметры ядра (Sysctl)

Файл: `/etc/sysctl.d/99-amneziawg-security.conf`. Включает `ip_forward`, отключает IPv6 (опц.), защиту от спуфинга (`rp_filter`), оптимизации TCP (`bbr`, `fq`, `syncookies`), отключает ICMP redirects.

<a id="fail2ban-adv"></a>
### Fail2Ban (Автоматическая установка)

* Скрипт автоматически устанавливает `fail2ban` и создает базовую конфигурацию `/etc/fail2ban/jail.local` для защиты SSH.
* **Настройки:** Бан через `ufw`, 3 попытки -> бан на 12 часов.
* **Проверка:** `sudo fail2ban-client status sshd`.
* **Тонкая настройка:** Редактируйте `/etc/fail2ban/jail.local` и перезапускайте `sudo systemctl restart fail2ban`.

---

<a id="automation-adv"></a>
## 🔄 Автоматизация обслуживания (Ограниченная)

Автоматическая настройка некоторых компонентов (резервное копирование по расписанию, авто-обновления системы, ротация логов) была **удалена** из скрипта установки для повышения стабильности.

Рекомендуется настраивать эти функции вручную при необходимости:

* **Резервное копирование:** Используйте команду `backup` в `manage_amneziawg.sh` вручную или через `cron`.
* **Автоматические обновления (`unattended-upgrades`):**
    ```bash
    sudo apt update && sudo apt install unattended-upgrades apt-listchanges
    sudo dpkg-reconfigure -plow unattended-upgrades
    ```
* **Ротация логов (`logrotate`):** Создайте файл в `/etc/logrotate.d/` для `/root/awg/*.log`.

---

<a id="cli-params-adv"></a>
## 🖥️ CLI Параметры запуска скриптов

<a id="install-cli-adv"></a>
### install_amneziawg.sh (v3.4+)

```
Опции:
  -h, --help            Показать справку
  --uninstall           Удалить AmneziaWG
  --diagnostic          Создать диагностический отчет
  -v, --verbose         Расширенный вывод (включая DEBUG)
  --no-color            Отключить цветной вывод
  --port=НОМЕР          Установить UDP порт (1024-65535)
  --subnet=ПОДСЕТЬ      Установить подсеть туннеля (x.x.x.x/yy)
  --allow-ipv6          Оставить IPv6 включенным
  --disallow-ipv6       Принудительно отключить IPv6
  --route-all           Режим: Весь трафик (0.0.0.0/0)
  --route-amnezia       Режим: Список Amnezia+DNS (умолч.)
  --route-custom=СЕТИ   Режим: Только указанные сети
```

<a id="manage-cli-adv"></a>
### manage_amneziawg.sh (v3.0+)

```
Опции:
  -h, --help            Показать справку
  -v, --verbose         Расширенный вывод (для list)
  --no-color            Отключить цветной вывод
  --conf-dir=ПУТЬ       Указать директорию AWG (умолч: /root/awg)
  --server-conf=ПУТЬ    Указать файл конфига сервера (умолч: /etc/amnezia/amneziawg/awg0.conf)
```

---

<a id="manage-commands-adv"></a>
## 🧑‍💻 Полный список команд управления

Используйте `sudo bash /root/awg/manage_amneziawg.sh <команда>`:

* **`add <имя>`:** Добавить клиента.
* **`remove <имя>`:** Удалить клиента.
* **`list [-v]`:** Список клиентов (с деталями при `-v`).
* **`regen [имя]`:** Перегенерировать файлы `.conf`/`.png` для клиента(ов).
* **`modify <имя> <пар> <зн>`:** Изменить параметр клиента в `.conf` файле.
* **`backup`:** Создать резервную копию.
* **`restore [файл]`:** Восстановить из резервной копии.
* **`check` / `status`:** Проверить состояние сервера.
* **`show`:** Выполнить `awg show`.
* **`restart`:** Перезапустить сервис AmneziaWG.
* **`help`:** Показать справку.

---

<a id="tech-details-adv"></a>
## 🛠️ Технические детали

* <a id="dkms-adv"></a>**DKMS:** Обеспечивает пересборку модуля ядра `amneziawg` при обновлении.
* <a id="python-venv-adv"></a>**Python Venv:** Изолированное окружение (`/root/awg/venv`) для `awgcfg.py`.
* <a id="awgcfgpy-workaround-adv"></a>**awgcfg.py и Workaround:** Внешний Python скрипт для генерации конфигов/ключей/QR. **Обнаружен баг:** вызов `awgcfg.py -c -q` (генерация файлов клиентов) **удаляет файл настроек** `/root/awg/awgsetup_cfg.init`. Для обхода проблемы, скрипты `install_amneziawg.sh` и `manage_amneziawg.sh` (команды `add`, `regen`) **временно перемещают** `awgsetup_cfg.init` перед вызовом `-c -q` и **возвращают его обратно** после.

---

<a id="faq-advanced-adv"></a>
## ❓ FAQ (Дополнительные вопросы)

<details>
  <summary><strong>В: Как изменить порт AmneziaWG после установки?</strong></summary>
  **О:** 1. Измените `ListenPort` в `/etc/amnezia/amneziawg/awg0.conf`. 2. Измените `AWG_PORT` в `/root/awg/awgsetup_cfg.init`. 3. Обновите UFW (`sudo ufw delete allow <старый_порт>/udp`, `sudo ufw allow <новый_порт>/udp`). 4. Перезапустите сервис (`sudo systemctl restart awg-quick@awg0`). 5. **Перегенерируйте конфиги ВСЕХ клиентов** (`sudo bash /root/awg/manage_amneziawg.sh regen`) и передайте их клиентам.
</details>

<details>
  <summary><strong>В: Как изменить внутреннюю подсеть VPN?</strong></summary>
  **О:** Проще всего выполнить деинсталляцию (`sudo bash ./install_amneziawg.sh --uninstall`) и установить заново, указав новую подсеть при первом запуске.
</details>

<details>
  <summary><strong>В: Как изменить MTU?</strong></summary>
  **О:** Добавьте строку `MTU = <значение>` (например, `MTU = 1420`) в секцию `[Interface]` файла `/etc/amnezia/amneziawg/awg0.conf` (для сервера) и/или в соответствующие `.conf` файлы клиентов. Перезапустите сервис и переподключите клиентов.
</details>

<details>
  <summary><strong>В: Почему используется Python и venv?</strong></summary>
  **О:** Внешний скрипт `awgcfg.py` написан на Python и требует библиотеку `qrcode` для генерации QR-кодов. Использование `venv` гарантирует, что установка зависимостей не повлияет на системные пакеты Python.
</details>

---

<a id="diag-uninstall-adv"></a>
## 🩺 Диагностика и деинсталляция

* **Диагностика:** `sudo bash /путь/к/install_amneziawg.sh --diagnostic`. Отчет в `/root/awg/diag_*.txt`.
* **Деинсталляция:** `sudo bash /путь/к/install_amneziawg.sh --uninstall`. Запросит подтверждение.

---

<a id="contributing-adv"></a>
## 🤝 Внесение вклада (Contributing)

Предложения и исправления приветствуются! Создавайте Issue или Pull Request в [репозитории](https://github.com/bivlked/amneziawg-installer).

---

<a id="thanks-adv"></a>
## 💖 Благодарности

* Команде [Amnezia VPN](https://github.com/amnezia-vpn).
* Пользователю [remittor](https://gist.github.com/remittor).

---

<p align="center">
  <a href="#amneziawg-installer-дополнительная-информация-и-настройки">↑ К началу</a>
</p>
