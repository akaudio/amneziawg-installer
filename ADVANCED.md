# AmneziaWG Installer: Дополнительная информация и настройки

Это дополнение к основному [README.md](README.md), содержащее более глубокие технические детали, пояснения и продвинутые опции для скриптов установки и управления AmneziaWG.

## Оглавление

<a id="toc"></a>
- [✨ Возможности (Подробно)](#features-detailed)
- [⚙️ Детали конфигурации клиента](#config-details)
  - [AllowedIPs](#allowedips)
  - [PersistentKeepalive](#persistentkeepalive)
  - [DNS](#dns)
  - [Изменение настроек по умолчанию](#change-defaults)
- [🔒 Настройки безопасности сервера](#security)
  - [Фаервол UFW](#ufw)
  - [Параметры ядра (Sysctl)](#sysctl)
  - [Fail2Ban (Автоматическая установка)](#fail2ban)
- [🔄 Автоматизация обслуживания (Убрано из авто-установки)](#automation)
- [ CLI Параметры запуска скриптов](#cli-params)
  - [install_amneziawg.sh](#install-cli)
  - [manage_amneziawg.sh](#manage-cli)
- [🧑‍💻 Полный список команд управления](#manage-commands)
- [🛠️ Технические детали](#tech-details)
  - [DKMS](#dkms)
  - [Python Venv](#python-venv)
  - [awgcfg.py и Workaround](#awgcfgpy-workaround)
- [❓ FAQ (Дополнительные вопросы)](#faq-advanced)
- [🩺 Диагностика и деинсталляция](#diag-uninstall)
- [🤝 Внесение вклада (Contributing)](#contributing)
- [💖 Благодарности](#thanks)

---

<a id="features-detailed"></a>
## ✨ Возможности (Подробно)

* **Автоматическая установка:** Устанавливает AmneziaWG, DKMS модуль, зависимости, Python окружение, настраивает сеть, фаервол `UFW`, параметры ядра `sysctl`.
* **Возобновляемость:** Использует файл `/root/awg/setup_state` для продолжения после обязательных перезагрузок.
* **Интерактивность:** При первом запуске запрашивает основные параметры (порт, подсеть, IPv6, режим маршрутизации), позволяя использовать значения по умолчанию.
* **Безопасность по умолчанию:**
    * `UFW`: Включается с политикой `deny incoming`, разрешает только SSH (с `limit` от брутфорса) и порт VPN.
    * `IPv6`: По умолчанию предлагается отключить для избежания утечек трафика.
    * `Права доступа`: Устанавливаются строгие права (600/700) на ключи, конфигурации и скрипты.
    * `Sysctl`: Применяются параметры для harden'инга TCP/IP, защиты от спуфинга и ICMP-атак.
    * `Fail2Ban`: **Автоматически** устанавливается и настраивается для защиты SSH.
* **Надежность:** Установка модуля ядра через DKMS, проверки `vermagic`.
* **Гибкость:** Возможность задать все ключевые параметры через аргументы командной строки для неинтерактивной установки.
* **Управление:** Скрипт `manage_amneziawg.sh` для управления клиентами, бэкапами, проверки статуса. Скачивается автоматически.
* **Резервное копирование:** Команда `backup` в скрипте управления. *Примечание: Автоматическое ежедневное копирование было удалено из установщика для повышения стабильности.*
* **Автоматизация:** Автоматическая установка Fail2Ban. *Примечание: Авто-обновления и ротация логов были удалены из установщика для повышения стабильности.*
* **Диагностика и Деинсталляция:** Опции `--diagnostic` и `--uninstall` у скрипта установки.

---

<a id="config-details"></a>
## ⚙️ Детали конфигурации клиента

<a id="allowedips"></a>
### AllowedIPs

Определяет, какой трафик клиент направляет в VPN.

1.  **Режим 1: Весь трафик (`0.0.0.0/0`)**
    * Весь IPv4 трафик клиента -> VPN.
    * Максимальная приватность. Может блокировать доступ к LAN.

2.  **Режим 2: Список Amnezia + DNS (По умолчанию)**
    * Список публичных IP-диапазонов + DNS `1.1.1.1`, `8.8.8.8`.
    * **Цель:** Обход DPI, туннелирование DNS. Рекомендуется.
    * Список: `0.0.0.0/5, ..., 8.8.8.8/32, 1.1.1.1/32`

3.  **Режим 3: Пользовательский (Split-Tunneling)**
    * Только трафик к указанным сетям -> VPN.
    * Пример: `192.168.1.0/24,10.50.0.0/16`

**Калькулятор AllowedIPs:** Для сложных правил: [WireGuard AllowedIPs Calculator](https://www.procustodibus.com/blog/2021/03/wireguard-allowedips-calculator/).

<a id="persistentkeepalive"></a>
### PersistentKeepalive

* **Значение по умолчанию:** `33` секунды.
* Поддерживает UDP-сессию через NAT.
* **Изменение:** `sudo bash /root/awg/manage_amneziawg.sh modify <имя> PersistentKeepalive 25`

<a id="dns"></a>
### DNS

* **Значение по умолчанию:** `1.1.1.1` (Cloudflare).
* DNS-сервер для клиента внутри VPN.
* **Изменение:** `sudo bash /root/awg/manage_amneziawg.sh modify <имя> DNS "8.8.8.8,1.0.0.1"`

<a id="change-defaults"></a>
### Изменение настроек по умолчанию

Отредактируйте скрипт **`install_amneziawg.sh`** *перед* первым запуском:

1.  Найдите `step6_generate_configs` -> команды `sed` для `DNS`, `PersistentKeepalive`.
2.  Найдите `configure_routing_mode` -> переменная `ALLOWED_IPS` в блоке `*)` для режима 2.

---

<a id="security"></a>
## 🔒 Настройки безопасности сервера

<a id="ufw"></a>
### Фаервол UFW

* **Политики:** Deny incoming, Allow outgoing.
* **Правила:** `limit 22/tcp` (SSH), `allow <порт_vpn>/udp`.
* **Проверка:** `sudo ufw status verbose`

<a id="sysctl"></a>
### Параметры ядра (Sysctl)

Файл: `/etc/sysctl.d/99-amneziawg-security.conf`. Включает `ip_forward`, отключает IPv6 (опц.), защиту от спуфинга (`rp_filter`), оптимизации TCP (`bbr`, `fq`, `syncookies`), отключает ICMP redirects.

<a id="fail2ban"></a>
### Fail2Ban (Автоматическая установка)

* Скрипт автоматически устанавливает `fail2ban` и создает базовую конфигурацию `/etc/fail2ban/jail.local` для защиты SSH.
* **Настройки:** Бан через `ufw`, 3 попытки -> бан на 12 часов.
* **Проверка:** `sudo fail2ban-client status sshd`.
* **Тонкая настройка:** Редактируйте `/etc/fail2ban/jail.local` и перезапускайте `sudo systemctl restart fail2ban`.

---

<a id="automation"></a>
## 🔄 Автоматизация обслуживания

<a id="backup"></a>
### Резервное копирование

* Скрипт `manage_amneziawg.sh` включает команды `backup` и `restore`.
* **Автоматическое ежедневное копирование и ротация старых копий были удалены** из скрипта установки (`install_amneziawg.sh`) из-за необъяснимых проблем со стабильностью и удалением файла конфигурации во время тестов.
* **Рекомендация:** Регулярно выполняйте `sudo bash /root/awg/manage_amneziawg.sh backup` вручную или настройте свое cron-задание для запуска этого скрипта или скрипта `/usr/local/bin/backup_amneziawg.sh` (если он был создан предыдущими версиями).

<a id="other-automation"></a>
### Прочие (Авто-обновления, Ротация логов)

* **Авто-обновления (`unattended-upgrades`) и Ротация логов (`logrotate`):** Автоматическая настройка этих функций также была **удалена** из скрипта установки для повышения стабильности.
* **Рекомендация:** Если вам необходимы эти функции, настройте их вручную стандартными средствами Ubuntu:
    * **Авто-обновления:** `sudo apt install unattended-upgrades && sudo dpkg-reconfigure -plow unattended-upgrades`
    * **Ротация логов:** Создайте файл конфигурации в `/etc/logrotate.d/` для нужных лог-файлов (`/root/awg/*.log`).

---

<a id="cli-params"></a>
## <0xF0><0x9F><0x96><0xA5>️ Параметры запуска скриптов

<a id="install-cli"></a>
### install_amneziawg.sh (v3.4+)

```
Опции:
  -h, --help            Показать справку
  --uninstall           Удалить AmneziaWG
  --diagnostic          Создать диагностический отчет
  -v, --verbose         Расширенный вывод (включая DEBUG)
  --no-color            Отключить цветной вывод
  --port=НОМЕР          Установить UDP порт (1024-65535)
  --subnet=ПОДСЕТЬ      Установить подсеть туннеля (x.x.x.x/yy)
  --allow-ipv6          Оставить IPv6 включенным
  --disallow-ipv6       Принудительно отключить IPv6
  --route-all           Режим: Весь трафик (0.0.0.0/0)
  --route-amnezia       Режим: Список Amnezia+DNS (умолч.)
  --route-custom=СЕТИ   Режим: Только указанные сети
```

<a id="manage-cli"></a>
### manage_amneziawg.sh (v3.0+)

```
Опции:
  -h, --help            Показать справку
  -v, --verbose         Расширенный вывод (для list)
  --no-color            Отключить цветной вывод
  --conf-dir=ПУТЬ       Указать директорию AWG (умолч: /root/awg)
  --server-conf=ПУТЬ    Указать файл конфига сервера (умолч: /etc/amnezia/amneziawg/awg0.conf)
```

---

<a id="manage-commands"></a>
## 🧑‍💻 Полный список команд управления

Используйте `sudo bash /root/awg/manage_amneziawg.sh <команда>`:

* **`add <имя>`:** Добавить клиента.
* **`remove <имя>`:** Удалить клиента.
* **`list [-v]`:** Список клиентов (с деталями при `-v`).
* **`regen [имя]`:** Перегенерировать файлы `.conf`/`.png` для клиента(ов).
* **`modify <имя> <пар> <зн>`:** Изменить параметр клиента в `.conf` файле.
* **`backup`:** Создать резервную копию конфигураций.
* **`restore [файл]`:** Восстановить конфигурации из бэкапа.
* **`check` / `status`:** Проверить состояние сервера.
* **`show`:** Выполнить `awg show`.
* **`restart`:** Перезапустить сервис AmneziaWG.
* **`help`:** Показать справку.

---

<a id="tech-details"></a>
## 🛠️ Технические детали

* **DKMS:** Обеспечивает пересборку модуля ядра `amneziawg` при обновлении.
* **Python Venv:** Изолированное окружение (`/root/awg/venv`) для `awgcfg.py`.
* **awgcfg.py и Workaround:** Внешний Python скрипт для генерации конфигов/ключей/QR. **В ходе тестов было обнаружено, что вызов `awgcfg.py -c -q` (генерация файлов клиентов) приводит к удалению файла настроек `/root/awg/awgsetup_cfg.init`.** Причина этого бага неизвестна. Для обхода проблемы, скрипты `install_amneziawg.sh` и `manage_amneziawg.sh` (при командах `add` и `regen`) **временно перемещают** `awgsetup_cfg.init` перед вызовом `-c -q` и **возвращают его обратно** после.

---

<a id="faq-advanced"></a>
## ❓ FAQ (Дополнительные вопросы)

<details>
  <summary><strong>В: Как изменить порт AmneziaWG после установки?</strong></summary>
  **О:** 1. Измените `ListenPort` в `/etc/amnezia/amneziawg/awg0.conf`. 2. Измените `AWG_PORT` в `/root/awg/awgsetup_cfg.init`. 3. Обновите UFW (`sudo ufw delete allow <старый_порт>/udp`, `sudo ufw allow <новый_порт>/udp`). 4. Перезапустите сервис (`sudo systemctl restart awg-quick@awg0`). 5. **Перегенерируйте конфиги ВСЕХ клиентов** (`sudo bash /root/awg/manage_amneziawg.sh regen`) и передайте их клиентам.
</details>

<details>
  <summary><strong>В: Как изменить внутреннюю подсеть VPN?</strong></summary>
  **О:** Проще всего выполнить деинсталляцию (`sudo bash ./install_amneziawg.sh --uninstall`) и установить заново, указав новую подсеть при первом запуске.
</details>

<details>
  <summary><strong>В: Как изменить MTU?</strong></summary>
  **О:** Добавьте строку `MTU = <значение>` (например, `MTU = 1420`) в секцию `[Interface]` файла `/etc/amnezia/amneziawg/awg0.conf` (для сервера) и/или в соответствующие `.conf` файлы клиентов. После этого перезапустите сервис (`sudo systemctl restart awg-quick@awg0`) и переподключите клиентов.
</details>

---

<a id="diag-uninstall"></a>
## 🩺 Диагностика и деинсталляция

* **Диагностика:** `sudo bash /путь/к/install_amneziawg.sh --diagnostic`. Отчет в `/root/awg/diag_*.txt`.
* **Деинсталляция:** `sudo bash /путь/к/install_amneziawg.sh --uninstall`. Запросит подтверждение.

---

<a id="contributing"></a>
## 🤝 Внесение вклада (Contributing)

Предложения и исправления приветствуются! Создавайте Issue или Pull Request в [репозитории](https://github.com/bivlked/amneziawg-installer).

---

<a id="thanks"></a>
## 💖 Благодарности

* Команде [Amnezia VPN](https://github.com/amnezia-vpn).
* Пользователю [remittor](https://gist.github.com/remittor).

---

<p align="center">
  <a href="#amneziawg-installer-дополнительная-информация-и-настройки">↑ К началу</a>
</p>
