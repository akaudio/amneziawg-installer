![Ubuntu 24.04](https://img.shields.io/badge/Ubuntu-24.04-orange)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://opensource.org/licenses/MIT) <!-- Пример лицензии MIT -->
![Status](https://img.shields.io/badge/Status-Active-success)

# Автоматическая установка AmneziaWG на Ubuntu 24.04 LTS

Этот репозиторий содержит набор Bash-скриптов для автоматизации установки, настройки и последующего управления VPN-сервером AmneziaWG на чистой системе Ubuntu Server 24.04 LTS Minimal.

## Возможности

*   🚀 **Быстрая установка:** Развертывание сервера одной командой.
*   🔄 **Возобновляемость:** Установку можно безопасно прерывать (например, для перезагрузки) и возобновлять повторным запуском скрипта.
*   🔒 **Безопасность:**
    *   Автоматическая настройка фаервола `UFW` (разрешены только SSH и порт VPN).
    *   Отключение IPv6 для уменьшения поверхности атаки.
    *   Установка строгих прав доступа к ключам и конфигурациям.
*   ⚙️ **Автоматическая конфигурация:**
    *   Включение репозиториев `deb-src`, необходимых для сборки DKMS модуля.
    *   Установка всех зависимостей (`dkms`, `build-essential`, `linux-headers` и т.д.).
    *   Проверка и автозагрузка модуля ядра `amneziawg`.
*   ✨ **Оптимизированные настройки клиента (по умолчанию):**
    *   DNS: `1.1.1.1` (Cloudflare).
    *   PersistentKeepalive: `33` секунды.
    *   AllowedIPs: Список диапазонов Amnezia для совместимости + принудительное туннелирование DNS Google/Cloudflare (см. [Детали конфигурации](#4-детали-конфигурации-и-настройки)).
*   🧑‍💻 **Удобное управление клиентами:** Отдельный скрипт (`manage_amneziawg.sh`) для добавления, удаления, просмотра клиентов и перегенерации их `.conf` / `.png` файлов.
*   📝 **Подробное логирование:** Все шаги установки и управления записываются в лог-файлы (`/root/awg/*.log`).

## Быстрый старт

Подключитесь к вашему **чистому серверу Ubuntu 24.04 LTS Minimal** по SSH и выполните команду:

```bash
wget -O - https://raw.githubusercontent.com/bivlked/amneziawg-installer/main/install_amneziawg.sh | sudo bash
```

**ВНИМАНИЕ:** Эта команда скачает и немедленно запустит скрипт установки с правами `root`. Убедитесь, что вы доверяете источнику скрипта (`raw.githubusercontent.com/bivlked/...`).

## 1. Требования к системе

Перед запуском скрипта установки убедитесь, что выполнены следующие условия:

*   **Операционная система:** Чистая установка **Ubuntu Server 24.04 LTS Minimal**. Наличие другого серверного ПО (веб-серверы, панели управления, другие VPN) может вызвать непредсказуемые конфликты.
*   **Доступ:** Доступ к серверу с правами `root` (через пользователя с `sudo` или прямой логин `root`).
*   **Интернет:** Стабильное подключение к Интернету для скачивания пакетов (~70 МБ для зависимостей + обновления системы).
*   **Ресурсы:**
    *   **Диск:** Не менее 2-3 ГБ свободного места (для ОС, обновлений, зависимостей, Python venv и логов).
    *   **ОЗУ:** Рекомендуется 512 МБ или больше.
*   **SSH Доступ:** Вы должны иметь возможность подключаться к серверу по SSH. Скрипт автоматически разрешит стандартный порт `22/tcp` в фаерволе UFW.
    *   ⚠️ **Нестандартный порт SSH:** Если ваш SSH-сервер слушает порт, отличный от 22, **ОБЯЗАТЕЛЬНО** добавьте для него разрешающее правило в UFW *вручную* **ДО** того, как скрипт дойдет до Шага 4 (настройка фаервола). Иначе вы **потеряете доступ** к серверу после включения UFW! Пример команды: `sudo ufw allow ВАШ_ПОРТ/tcp`.

## 2. Установка AmneziaWG (`install_amneziawg.sh`)

Этот скрипт выполняет полную первичную установку и настройку сервера AmneziaWG.

**Порядок действий:**

1.  **Запустите скрипт ОДНОЙ КОМАНДОЙ:**
    ```bash
    wget -O - https://raw.githubusercontent.com/bivlked/amneziawg-installer/main/install_amneziawg.sh | sudo bash
    ```

2.  **Начальная настройка (при первом запуске):**
    *   Скрипт обнаружит, что файл конфигурации `/root/awg/setup.conf` отсутствует.
    *   Он запросит у вас **UDP порт** для AmneziaWG. Нажмите `Enter`, чтобы использовать порт по умолчанию (`39743`), или введите свой (число от 1024 до 65535) и нажмите `Enter`.
    *   Затем он запросит **внутреннюю подсеть** для VPN-клиентов. Нажмите `Enter` для использования подсети по умолчанию (`10.9.9.1/24`) или введите свою (например, `10.20.30.1/24`) и нажмите `Enter`.
    *   Введенные или стандартные настройки будут сохранены в `/root/awg/setup.conf` для последующих запусков скрипта.

3.  **Следуйте инструкциям на экране:**
    *   Скрипт будет подробно логировать свои действия.
    *   **Перезагрузки:** В процессе установки потребуется **ДВЕ** перезагрузки (после обновления системы и после установки DKMS модуля). Скрипт **автоматически обнаружит** эту необходимость, выведет **ЗАМЕТНОЕ** предупреждение и запросит у вас подтверждение:
      ```
      Перезагрузить сейчас? [y/N]:
      ```
      Введите `y` и нажмите `Enter`, чтобы перезагрузить сервер. Если вы введете `N` или просто нажмете `Enter`, скрипт завершится, и вам нужно будет перезагрузить сервер вручную.

4.  **Продолжение после перезагрузки:**
    *   **ВАЖНО!** После каждой перезагрузки вам нужно будет **снова подключиться к серверу** по SSH и **запустить ту же самую команду**, что и в самом начале:
        ```bash
        wget -O - https://raw.githubusercontent.com/bivlked/amneziawg-installer/main/install_amneziawg.sh | sudo bash
        ```
    *   Скрипт прочитает сохраненное состояние из `/root/awg/setup_state` и автоматически продолжит выполнение с необходимого шага.

5.  **Завершение установки:**
    *   После успешного выполнения всех шагов скрипт выведет сообщение `Установка и настройка AmneziaWG УСПЕШНО ЗАВЕРШЕНА!`.
    *   Он также покажет расположение клиентских файлов, полезные команды для управления сервером и инструкцию по скачиванию и использованию скрипта управления `manage_amneziawg.sh`.
    *   Файл состояния `/root/awg/setup_state` будет автоматически удален.
    *   Полный лог установки останется в файле `/root/awg/install_amneziawg.log`.

## 3. Управление клиентами (`manage_amneziawg.sh`)

После успешной установки сервера используйте скрипт `manage_amneziawg.sh` для добавления, удаления и других операций с клиентскими конфигурациями. Скрипт установки **автоматически скачивает** `manage_amneziawg.sh` в директорию `/root/awg` и делает его исполняемым на Шаге 5.

**Использование:**

Все команды выполняются с правами `root`:

```bash
sudo bash /root/awg/manage_amneziawg.sh <команда> [аргументы]
```

**Доступные команды:**

*   `add <имя_клиента>`
    *   Добавляет нового клиента (пира) с указанным именем.
    *   Имя может содержать латинские буквы, цифры, дефис (`-`) и подчеркивание (`_`).
    *   *Пример:* `sudo bash /root/awg/manage_amneziawg.sh add work-pc`
    *   *Результат:* Клиент добавляется в `/etc/amnezia/amneziawg/awg0.conf`. Файлы `<имя_клиента>.conf` и `<имя_клиента>.png` создаются/обновляются в `/root/awg`.
    *   **❗️ Действие после команды:** **Перезапустите сервис AmneziaWG**:
        ```bash
        sudo systemctl restart awg-quick@awg0
        ```

*   `remove <имя_клиента>`
    *   Удаляет существующего клиента.
    *   *Пример:* `sudo bash /root/awg/manage_amneziawg.sh remove old-phone`
    *   *Результат:* Клиент удаляется из `/etc/amnezia/amneziawg/awg0.conf`. Файлы `<имя_клиента>.conf` и `<имя_клиента>.png` удаляются из `/root/awg`.
    *   **❗️ Действие после команды:** **Перезапустите сервис AmneziaWG**:
        ```bash
        sudo systemctl restart awg-quick@awg0
        ```

*   `list`
    *   Показывает список клиентов, найденных в конфигурации сервера, и отмечает наличие (`✓`) или отсутствие (`-`) их `.conf` и `.png` файлов в директории `/root/awg`.
    *   *Пример:* `sudo bash /root/awg/manage_amneziawg.sh list`

*   `regen [имя_клиента]`
    *   Перегенерирует `.conf` файл и QR-код (`.png`). Ключи клиента при этом **не меняются**. Полезно, если вы случайно удалили файл или хотите применить обновленные настройки из шаблона.
    *   Если `<имя_клиента>` **не указано**, файлы будут перегенерированы для **всех** клиентов, существующих в конфигурации сервера.
    *   Если `<имя_клиента>` **указано**, файлы будут перегенерированы также для **всех** клиентов (из-за ограничений утилиты `awgcfg.py`), но скрипт выведет сообщение об успешной перегенерации именно для указанного клиента.
    *   *Пример (для одного, но перегенерит все):* `sudo bash /root/awg/manage_amneziawg.sh regen my_laptop`
    *   *Пример (для всех):* `sudo bash /root/awg/manage_amneziawg.sh regen`
    *   *Результат:* Файлы `.conf` и `.png` в `/root/awg` будут перезаписаны.

*   `show`
    *   Показывает текущий статус интерфейса AmneziaWG, включая прослушиваемый порт, публичный ключ сервера и информацию об активных пирах (аналогично команде `awg show`).
    *   *Пример:* `sudo bash /root/awg/manage_amneziawg.sh show`

*   `help`
    *   Показывает краткую справку по командам.

**Расположение файлов клиентов:**

Все генерируемые файлы конфигурации (`.conf`) и QR-коды (`.png`) для клиентов находятся в директории `/root/awg/`. Используйте безопасные методы (например, `scp`, `sftp` или подключение по SFTP через файловый менеджер) для их копирования на ваши клиентские устройства. **Обращайтесь с этими файлами осторожно, так как они содержат приватные ключи доступа к VPN!**

## 4. Детали конфигурации и настройки

Этот раздел содержит более подробную информацию о некоторых аспектах установки и конфигурации.

*   **Идемпотентность и Возобновляемость:** Скрипт установки `install_amneziawg.sh` использует файл состояния `/root/awg/setup_state` для отслеживания прогресса. При каждом запуске он читает этот файл и продолжает с незавершенного шага. Пользовательские настройки (порт, подсеть), введенные при первом запуске, хранятся в `/root/awg/setup.conf` и используются при последующих запусках. Это позволяет безопасно перезапускать скрипт после перезагрузок или сбоев.
*   **Автоматизация:** Скрипт автоматизирует множество рутинных и подверженных ошибкам задач:
    *   **Включение `deb-src`:** Репозитории с исходными кодами, необходимые для корректной сборки DKMS-модуля AmneziaWG при обновлениях ядра, автоматически включаются в `/etc/apt/sources.list.d/ubuntu.sources`.
    *   **Установка зависимостей:** Устанавливаются все необходимые пакеты: `build-essential`, `dkms`, `linux-headers` для текущего ядра, `python3-venv`, `python3-pip`, `ufw` и сами пакеты `amneziawg`.
    *   **Загрузка модуля:** Проверяется загрузка модуля ядра `amneziawg`, при необходимости выполняется принудительная загрузка (`modprobe`) и добавление в автозагрузку через `/etc/modules-load.d/`.
*   **Безопасность:**
    *   **Фаервол (UFW):** Настраивается с политикой "запретить всё входящее по умолчанию" (`default deny incoming`). Явно разрешаются только входящие соединения для стандартного порта SSH (`22/tcp`) и указанного пользователем UDP-порта для AmneziaWG. Весь исходящий трафик разрешен по умолчанию (`default allow outgoing`).
    *   **Права доступа:** На директорию `/etc/amnezia` и ее поддиректории устанавливаются права `700` (только владелец `root` имеет доступ). На файл конфигурации сервера `/etc/amnezia/amneziawg/awg0.conf`, содержащий приватный ключ сервера, устанавливаются права `600` (только `root` может читать и писать).
    *   **Отключение IPv6:** Уменьшает потенциальную поверхность атаки и предотвращает возможные утечки трафика, если клиент или сервер попытаются использовать IPv6 в обход VPN.
*   **Кастомизация клиента (по умолчанию):** Скрипт установки автоматически модифицирует файл-шаблон `_defclient.config` перед генерацией конечных файлов `.conf`:
    *   **`DNS = 1.1.1.1`**: Устанавливает DNS-сервер Cloudflare для клиентов. Этот сервер известен своей скоростью и фокусом на приватности.
    *   **`PersistentKeepalive = 33`**: Задает интервал в 33 секунды для отправки keepalive-пакетов от клиента к серверу. Это помогает поддерживать UDP-сессию активной при прохождении через NAT и stateful-фаерволы, предотвращая разрывы соединения из-за неактивности. Значение 25-35 секунд является распространенной практикой.
    *   **`AllowedIPs = 0.0.0.0/5, ..., 8.8.8.8/32, 1.1.1.1/32`**: Этот параметр определяет, какой трафик **клиент** должен направлять **в VPN-туннель**. Вместо стандартного `0.0.0.0/0` (весь IPv4 трафик), скрипт по умолчанию использует сложный список IP-диапазонов, предложенный разработчиками AmneziaWG, с добавлением IP-адресов популярных DNS-серверов Google и Cloudflare.
        *   **Предполагаемая цель:** Усложнение детекции VPN-трафика системами DPI (Deep Packet Inspection) и потенциальное улучшение совместимости в сетях с ограничениями. Явное указание DNS-серверов (`/32`) гарантирует, что запросы к ним пойдут через туннель.
        *   **Ограничения:** Этот список **не предназначен** для реализации "split-tunneling" (раздельного туннелирования), т.е. он **не исключает** трафик в локальную сеть клиента (например, к принтеру или другим устройствам в сети Wi-Fi) или к специфическим веб-сайтам (например, государственным сервисам). Если вам требуется доступ к локальным ресурсам при активном VPN или исключение определенных сайтов из туннеля, вам необходимо:
            1.  **Изменить `AllowedIPs`:** Самый простой вариант — изменить значение `AllowedIPs` в клиентском `.conf` файле *после* его генерации на `0.0.0.0/0` (для полного туннелирования) или на конкретные IP-адреса/подсети, которые должны идти через VPN (для частичного туннелирования).
            2.  **(Продвинутый вариант)** Настроить маршрутизацию или правила фаервола на стороне клиента или сервера вручную.
        *   **Изменение по умолчанию:** Если вы хотите, чтобы скрипт *всегда* генерировал конфиги со стандартным `AllowedIPs = 0.0.0.0/0`, вам нужно отредактировать скрипт `install_amneziawg.sh` *перед* его первым запуском (или перед Шагом 6), найдя в функции `step6_generate_configs` строку с `sed -i "s#^AllowedIPs = .*#..."` и заменив сложный список на `AllowedIPs = 0.0.0.0/0`.

## 5. Принципы и инструменты

*   **Bash:** Скрипты написаны на Bash, стандартной командной оболочке Linux.
*   **APT:** Система управления пакетами Ubuntu используется для установки ПО.
*   **Systemd:** Система инициализации и управления службами используется для запуска и автозагрузки AmneziaWG (`awg-quick@awg0.service`).
*   **Sysctl:** Утилита для настройки параметров ядра Linux (отключение IPv6, включение IP forwarding).
*   **UFW (Uncomplicated Firewall):** Простой интерфейс для управления фаерволом `iptables`.
*   **DKMS (Dynamic Kernel Module Support):** Система для автоматической пересборки модуля ядра AmneziaWG при обновлении ядра системы.
*   **Python + Venv:** Используется для запуска утилиты `awgcfg.py` в изолированном виртуальном окружении с необходимыми библиотеками (`qrcode`, `pillow`).
*   **awgcfg.py:** Внешняя Python-утилита от пользователя `remittor` (изначально из Gist) для удобной генерации конфигураций AmneziaWG.
*   **awg / awg-quick:** Стандартные утилиты командной строки из пакета `amneziawg-tools`.
*   **Стандартные утилиты:** `grep`, `sed`, `wget`, `curl`, `chmod`, `chown`, `rm`, `cp`, `tee`, `awk`, `printf`, `cat`, `ls`, `cd`, `mkdir`, `touch`, `read`.
*   **Логирование:** Скрипты ведут текстовые логи в `/root/awg/`.

## 6. Отказ от ответственности

Данные скрипты и инструкции предоставляются "как есть", без каких-либо явных или подразумеваемых гарантий. Используйте их на свой страх и риск.

Перед использованием на производственных или критически важных системах **настоятельно рекомендуется** провести тестирование в изолированной виртуальной среде.

Автор и участники разработки не несут ответственности за любые прямые или косвенные убытки, потерю данных, блокировку доступа к системе или другие проблемы, возникшие в результате использования или неправильного использования этих скриптов.

**Всегда делайте резервные копии важных данных перед внесением значительных изменений в конфигурацию системы.**
