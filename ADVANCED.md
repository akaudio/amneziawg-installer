12345 # AmneziaWG Installer: Дополнительная информация и настройки
12345 
12345 Это дополнение к основному [README.md](README.md), содержащее более глубокие технические детали, пояснения и продвинутые опции для скриптов установки и управления AmneziaWG.
12345 
12345 ## Оглавление
12345 
12345 <a id="toc-adv"></a>
12345 - [✨ Возможности (Подробно)](#features-detailed-adv)
12345 - [⚙️ Детали конфигурации клиента](#config-details-adv)
12345   - [AllowedIPs](#allowedips-adv)
12345   - [PersistentKeepalive](#persistentkeepalive-adv)
12345   - [DNS](#dns-adv)
12345   - [Изменение настроек по умолчанию](#change-defaults-adv)
12345 - [🔒 Настройки безопасности сервера](#security-adv)
12345   - [Фаервол UFW](#ufw-adv)
12345   - [Параметры ядра (Sysctl)](#sysctl-adv)
12345   - [Fail2Ban (Автоматическая установка)](#fail2ban-adv)
12345 - [🔄 Автоматизация обслуживания (Ограниченная)](#automation-adv)
12345 - [ CLI Параметры запуска скриптов](#cli-params-adv)
12345   - [install_amneziawg.sh](#install-cli-adv)
12345   - [manage_amneziawg.sh](#manage-cli-adv)
12345 - [🧑‍💻 Полный список команд управления](#manage-commands-adv)
12345 - [🛠️ Технические детали](#tech-details-adv)
12345   - [DKMS](#dkms-adv)
12345   - [Python Venv](#python-venv-adv)
12345   - [awgcfg.py и Workaround](#awgcfgpy-workaround-adv)
12345 - [❓ FAQ (Дополнительные вопросы)](#faq-advanced-adv)
12345 - [🩺 Диагностика и деинсталляция](#diag-uninstall-adv)
12345 - [🤝 Внесение вклада (Contributing)](#contributing-adv)
12345 - [💖 Благодарности](#thanks-adv)
12345 
12345 ---
12345 
12345 <a id="features-detailed-adv"></a>
12345 ## ✨ Возможности (Подробно)
12345 
12345 * **Автоматическая установка:** Устанавливает AmneziaWG, DKMS модуль, необходимые зависимости, Python окружение, настраивает сеть, фаервол, sysctl.
12345 * **Возобновляемость:** Использует файл состояния (`/root/awg/setup_state`) для продолжения после обязательных перезагрузок.
12345 * **Интерактивность:** При первом запуске запрашивает основные параметры (порт, подсеть, IPv6, режим маршрутизации), позволяя использовать значения по умолчанию.
12345 * **Безопасность по умолчанию:**
12345     * `UFW`: Включается с политикой `deny incoming`, разрешает только SSH (с `limit` от брутфорса) и порт VPN.
12345     * `IPv6`: По умолчанию предлагается отключить через `sysctl` для предотвращения утечек.
12345     * `Права доступа`: Устанавливаются строгие права (600/700) на ключи, конфигурации и скрипты.
12345     * `Sysctl`: Применяются рекомендованные параметры для harden'инга TCP/IP стека, защиты от спуфинга и ICMP-атак.
12345     * `Fail2Ban`: **Автоматически** устанавливается и настраивается для защиты SSH.
12345 * **Надежность:** Установка модуля ядра через DKMS, проверки `vermagic`.
12345 * **Гибкость:** Возможность задать все ключевые параметры через аргументы командной строки для неинтерактивной установки.
12345 * **Управление:** Скрипт `manage_amneziawg.sh` для управления клиентами, бэкапами, проверки статуса и т.д.
12345 * **Резервное копирование:** Команда `backup` в скрипте управления для ручного создания бэкапов основных конфигураций.
12345 * **Диагностика и Деинсталляция:** Опции `--diagnostic` и `--uninstall` у скрипта установки.
12345 
12345 ---
12345 
12345 <a id="config-details-adv"></a>
12345 ## ⚙️ Детали конфигурации клиента
12345 
12345 <a id="allowedips-adv"></a>
12345 ### AllowedIPs
12345 
12345 Определяет, какой трафик **клиент** направляет в VPN-туннель.
12345 
12345 1.  **Режим 1: Весь трафик (`0.0.0.0/0`)**
12345     * Весь IPv4 трафик клиента -> VPN.
12345     * Максимальная приватность. Может блокировать доступ к LAN.
12345 
12345 2.  **Режим 2: Список Amnezia + DNS (По умолчанию)**
12345     * Список публичных IP-диапазонов + DNS `1.1.1.1`, `8.8.8.8`.
12345     * **Цель:** Обход DPI, туннелирование DNS. Рекомендуется.
12345     * Список: `0.0.0.0/5, ..., 8.8.8.8/32, 1.1.1.1/32`
12345 
12345 3.  **Режим 3: Пользовательский (Split-Tunneling)**
12345     * Только трафик к указанным сетям -> VPN.
12345     * Пример: `192.168.1.0/24,10.50.0.0/16`
12345 
12345 **Калькулятор AllowedIPs:** Для сложных правил: [WireGuard AllowedIPs Calculator](https://www.procustodibus.com/blog/2021/03/wireguard-allowedips-calculator/).
12345 
12345 <a id="persistentkeepalive-adv"></a>
12345 ### PersistentKeepalive
12345 
12345 * **Значение по умолчанию:** `33` секунды.
12345 * Поддерживает UDP-сессию через NAT.
12345 * **Изменение:** `sudo bash /root/awg/manage_amneziawg.sh modify <имя> PersistentKeepalive 25`
12345 
12345 <a id="dns-adv"></a>
12345 ### DNS
12345 
12345 * **Значение по умолчанию:** `1.1.1.1` (Cloudflare).
12345 * DNS-сервер для клиента внутри VPN.
12345 * **Изменение:** `sudo bash /root/awg/manage_amneziawg.sh modify <имя> DNS "8.8.8.8,1.0.0.1"`
12345 
12345 <a id="change-defaults-adv"></a>
12345 ### Изменение настроек по умолчанию
12345 
12345 Отредактируйте скрипт **`install_amneziawg.sh`** *перед* первым запуском:
12345 
12345 1.  Найдите `step6_generate_configs` -> команды `sed` для `DNS`, `PersistentKeepalive`.
12345 2.  Найдите `configure_routing_mode` -> переменная `ALLOWED_IPS` в блоке `*)` для режима 2.
12345 
12345 ---
12345 
12345 <a id="security-adv"></a>
12345 ## 🔒 Настройки безопасности сервера
12345 
12345 <a id="ufw-adv"></a>
12345 ### Фаервол UFW
12345 
12345 * **Политики:** Deny incoming, Allow outgoing.
12345 * **Правила:** `limit 22/tcp`, `allow <порт_vpn>/udp`.
12345 * **Проверка:** `sudo ufw status verbose`
12345 
12345 <a id="sysctl-adv"></a>
12345 ### Параметры ядра (Sysctl)
12345 
12345 Файл: `/etc/sysctl.d/99-amneziawg-security.conf`. Включает `ip_forward`, отключает IPv6 (опц.), защиту от спуфинга (`rp_filter`), оптимизации TCP (`bbr`, `fq`, `syncookies`), отключает ICMP redirects.
12345 
12345 <a id="fail2ban-adv"></a>
12345 ### Fail2Ban (Автоматическая установка)
12345 
12345 * Скрипт автоматически устанавливает `fail2ban` и создает базовую конфигурацию `/etc/fail2ban/jail.local` для защиты SSH.
12345 * **Настройки:** Бан через `ufw`, 3 попытки -> бан на 12 часов.
12345 * **Проверка:** `sudo fail2ban-client status sshd`.
12345 * **Тонкая настройка:** Редактируйте `/etc/fail2ban/jail.local` и перезапускайте `sudo systemctl restart fail2ban`.
12345 
12345 ---
12345 
12345 <a id="automation-adv"></a>
12345 ## 🔄 Автоматизация обслуживания (Ограниченная)
12345 
12345 Для повышения стабильности и предсказуемости работы основного скрипта установки, автоматическая настройка некоторых дополнительных компонентов (резервное копирование по расписанию, авто-обновления системы, ротация логов) была **удалена** из финальной версии скрипта `install_amneziawg.sh`.
12345 
12345 Однако, для удобства пользователей, скрипт управления `manage_amneziawg.sh` **включает** команду для ручного создания резервных копий.
12345 
12345 Если вам необходима полная автоматизация этих процессов, рекомендуется настроить их стандартными средствами Ubuntu:
12345 
12345 * **Автоматическое резервное копирование:** Используйте `cron` для регулярного запуска команды `sudo bash /root/awg/manage_amneziawg.sh backup`.
12345 * **Автоматические обновления (`unattended-upgrades`):**
12345     ```bash
12345     sudo apt update
12345     sudo apt install unattended-upgrades apt-listchanges
12345     sudo dpkg-reconfigure -plow unattended-upgrades
12345     # При необходимости настройте /etc/apt/apt.conf.d/50unattended-upgrades
12345     ```
12345 * **Ротация логов (`logrotate`):** Создайте файл `/etc/logrotate.d/amneziawg-custom` (или другое имя) с правилами ротации для `/root/awg/*.log`. Пример содержимого:
12345     ```
12345     /root/awg/install_amneziawg.log
12345     /root/awg/manage_amneziawg.log {
12345         weekly
12345         rotate 4
12345         compress
12345         delaycompress
12345         missingok
12345         notifempty
12345     }
12345     ```
12345 
12345 ---
12345 
12345 <a id="cli-params-adv"></a>
12345 ## <0xF0><0x9F><0x96><0xA5>️ Параметры запуска скриптов
12345 
12345 <a id="install-cli-adv"></a>
12345 ### install_amneziawg.sh (v3.4+)
12345 
12345 ```
12345 Опции:
12345   -h, --help            Показать справку
12345   --uninstall           Удалить AmneziaWG
12345   --diagnostic          Создать диагностический отчет
12345   -v, --verbose         Расширенный вывод (включая DEBUG)
12345   --no-color            Отключить цветной вывод
12345   --port=НОМЕР          Установить UDP порт (1024-65535)
12345   --subnet=ПОДСЕТЬ      Установить подсеть туннеля (x.x.x.x/yy)
12345   --allow-ipv6          Оставить IPv6 включенным
12345   --disallow-ipv6       Принудительно отключить IPv6
12345   --route-all           Режим: Весь трафик (0.0.0.0/0)
12345   --route-amnezia       Режим: Список Amnezia+DNS (умолч.)
12345   --route-custom=СЕТИ   Режим: Только указанные сети
12345 ```
12345 
12345 <a id="manage-cli-adv"></a>
12345 ### manage_amneziawg.sh (v3.0+)
12345 
12345 ```
12345 Опции:
12345   -h, --help            Показать справку
12345   -v, --verbose         Расширенный вывод (для list)
12345   --no-color            Отключить цветной вывод
12345   --conf-dir=ПУТЬ       Указать директорию AWG (умолч: /root/awg)
12345   --server-conf=ПУТЬ    Указать файл конфига сервера (умолч: /etc/amnezia/amneziawg/awg0.conf)
12345 ```
12345 
12345 ---
12345 
12345 <a id="manage-commands-adv"></a>
12345 ## 🧑‍💻 Полный список команд управления
12345 
12345 Используйте `sudo bash /root/awg/manage_amneziawg.sh <команда>`:
12345 
12345 * **`add <имя>`:** Добавить клиента. Генерирует ключи, обновляет конфиг сервера, создает `.conf` и `.png` клиента (используя обходной маневр для `awgsetup_cfg.init`). Требует перезапуска сервиса.
12345 * **`remove <имя>`:** Удалить клиента. Удаляет пира из конфига сервера, удаляет файлы клиента. Требует перезапуска сервиса.
12345 * **`list [-v]`:** Список клиентов. Показывает наличие файлов `.conf`/`.png`. С флагом `-v` показывает IP, начало ключа и статус активности по данным `awg show`.
12345 * **`regen [имя]`:** Перегенерировать файлы `.conf` и `.png` для указанного клиента или для **всех**, если имя не указано (ключи пиров не меняются, обновляется только информация о сервере и QR-код). Использует обходной маневр для `awgsetup_cfg.init`.
12345 * **`modify <имя> <пар> <зн>`:** Изменить параметр (`DNS`, `AllowedIPs`, `PersistentKeepalive` и т.д.) в файле `/root/awg/<имя>.conf`. Создает бэкап `.conf` файла перед изменением. Перегенерирует QR-код, если изменен важный параметр.
12345 * **`backup`:** Создать резервную копию (`/etc/amnezia/amneziawg/`, `/root/awg/*.conf`, `/root/awg/*.png`, `/root/awg/awgsetup_cfg.init`) в директорию `/root/awg/backups/`, сохраняя 10 последних копий.
12345 * **`restore [файл]`:** Восстановить конфигурацию из указанного файла бэкапа или интерактивно выбрать из последних 10. Создает бэкап текущей конфигурации перед восстановлением. Требует подтверждения и перезапуска сервиса.
12345 * **`check` / `status`:** Выполнить комплексную проверку состояния сервера (статус systemd, интерфейс, порт, forwarding, UFW, `awg show`).
12345 * **`show`:** Выполнить команду `awg show`.
12345 * **`restart`:** Перезапустить сервис `awg-quick@awg0` (с запросом подтверждения).
12345 * **`help`:** Показать справку по командам и опциям.
12345 
12345 ---
12345 
12345 <a id="tech-details-adv"></a>
12345 ## 🛠️ Технические детали
12345 
12345 * <a id="dkms-adv"></a>**DKMS:** Dynamic Kernel Module Support используется для автоматической пересборки модуля ядра `amneziawg` при обновлении системного ядра. Это обеспечивает совместимость после обновлений. Логи сборки: `/var/lib/dkms/amneziawg/`.
12345 * <a id="python-venv-adv"></a>**Python Venv:** Изолированное окружение (`/root/awg/venv`) для Python-скрипта `awgcfg.py` и его зависимостей (`qrcode`, `pillow`), чтобы не конфликтовать с системными пакетами Python.
12345 * <a id="awgcfgpy-workaround-adv"></a>**awgcfg.py и Workaround:** Внешний Python скрипт `awgcfg.py` (от `remittor`) используется для генерации ключей, конфигов и QR. **В ходе тестов было обнаружено, что вызов `awgcfg.py -c -q` (генерация файлов клиентов) приводил к необъяснимому удалению файла настроек `/root/awg/awgsetup_cfg.init`.** Причина этого бага неизвестна. Для обхода проблемы, скрипты `install_amneziawg.sh` (в Шаге 6) и `manage_amneziawg.sh` (при командах `add` и `regen`) **временно перемещают** `awgsetup_cfg.init` в `/root/` перед вызовом `-c -q` и **возвращают его обратно** после. Это гарантирует сохранность файла настроек.
12345 
12345 ---
12345 
12345 <a id="faq-advanced-adv"></a>
12345 ## ❓ FAQ (Дополнительные вопросы)
12345 
12345 <details>
12345   <summary><strong>В: Как изменить порт AmneziaWG после установки?</strong></summary>
12345   **О:** 1. Измените `ListenPort` в `/etc/amnezia/amneziawg/awg0.conf`. 2. Измените `AWG_PORT` в `/root/awg/awgsetup_cfg.init`. 3. Обновите UFW (`sudo ufw delete allow <старый_порт>/udp`, `sudo ufw allow <новый_порт>/udp`). 4. Перезапустите сервис (`sudo systemctl restart awg-quick@awg0`). 5. **Перегенерируйте конфиги ВСЕХ клиентов** (`sudo bash /root/awg/manage_amneziawg.sh regen`) и передайте их клиентам.
12345 </details>
12345 
12345 <details>
12345   <summary><strong>В: Как изменить внутреннюю подсеть VPN?</strong></summary>
12345   **О:** Проще всего выполнить деинсталляцию (`sudo bash ./install_amneziawg.sh --uninstall`) и установить заново, указав новую подсеть при первом запуске.
12345 </details>
12345 
12345 <details>
12345   <summary><strong>В: Как изменить MTU?</strong></summary>
12345   **О:** Добавьте строку `MTU = <значение>` (например, `MTU = 1420`) в секцию `[Interface]` файла `/etc/amnezia/amneziawg/awg0.conf` (для сервера) и/или в соответствующие `.conf` файлы клиентов. После этого перезапустите сервис (`sudo systemctl restart awg-quick@awg0`) и переподключите клиентов.
12345 </details>
12345 
12345 <details>
12345   <summary><strong>В: Почему используется Python и venv?</strong></summary>
12345   **О:** Внешний скрипт `awgcfg.py` написан на Python и требует библиотеку `qrcode` для генерации QR-кодов. Использование `venv` (виртуального окружения) гарантирует, что установка этих зависимостей не повлияет на системные пакеты Python и не вызовет конфликтов.
12345 </details>
12345 
12345 ---
12345 
12345 <a id="diag-uninstall-adv"></a>
12345 ## 🩺 Диагностика и деинсталляция
12345 
12345 * **Диагностика:** `sudo bash /путь/к/install_amneziawg.sh --diagnostic`. Отчет в `/root/awg/diag_*.txt`.
12345 * **Деинсталляция:** `sudo bash /путь/к/install_amneziawg.sh --uninstall`. Запросит подтверждение. Удаляет пакеты, конфиги, ключи, скрипты, правила UFW, модуль DKMS и другие файлы, созданные установщиком.
12345 
12345 ---
12345 
12345 <a id="contributing-adv"></a>
12345 ## 🤝 Внесение вклада (Contributing)
12345 
12345 Предложения и исправления приветствуются! Создавайте Issue или Pull Request в [репозитории](https://github.com/bivlked/amneziawg-installer).
12345 
12345 ---
12345 
12345 <a id="thanks-adv"></a>
12345 ## 💖 Благодарности
12345 
12345 * Команде [Amnezia VPN](https://github.com/amnezia-vpn).
12345 * Пользователю [remittor](https://gist.github.com/remittor).
12345 
12345 ---
12345 
12345 <p align="center">
12345   <a href="#amneziawg-installer-дополнительная-информация-и-настройки">↑ К началу</a>
12345 </p>
